import type { HistoricalLoad, HistoricalOffer, CarrierCapacity, Datasets, UpcomingLoad } from '../src/data/schemas'
export const AS_OF = '2026-09-07T08:00:00+03:00'
export const historyRow = (overrides: Partial<HistoricalLoad> = {}): HistoricalLoad => ({ load_id: 'h', date: '2026-09-06', customer: 'Customer', business_unit: 'Domestic', origin: 'A', destination: 'B', lane: 'a → b', equipment_type: 'flatbed', carrier_id: 'c', sell_rate: 1400, final_buy_rate: 1000, fulfilled: true, pickup_ontime: true, delivery_ontime: true, cancelled: false, failure_reason: null, currency: 'SAR', ...overrides })
export const offerRow = (overrides: Partial<HistoricalOffer> = {}): HistoricalOffer => ({ offer_id: 'o', load_id: null, date: '2026-09-06', lane: 'a → b', equipment_type: 'flatbed', carrier_id: 'c', offered_buy_rate: 1000, accepted: true, sell_rate: null, currency: 'SAR', ...overrides })
export const capacityRow = (overrides: Partial<CarrierCapacity> = {}): CarrierCapacity => ({ capacity_id: 'k', carrier_id: 'c', carrier_name: 'Carrier', lane: 'a → b', equipment_type: 'flatbed', capacity_date: '2026-09-07', available_trucks: 10, deadhead_km_to_origin: 150, active: true, contract_status: 'Spot', ...overrides })
export const upcomingRow = (overrides: Partial<UpcomingLoad> = {}): UpcomingLoad => ({ load_id: 'u', customer: 'Customer', business_unit: 'Domestic', origin: 'A', destination: 'B', lane: 'a → b', equipment_type: 'flatbed', pickup_datetime: '2026-09-07T20:00:00+03:00', sell_rate: 1400, planned_buy_rate: 1000, priority: 'Standard', load_count: 10, currency: 'SAR', ...overrides })
export function fixture(): Datasets { return { upcoming: [upcomingRow()], capacity: [capacityRow()], historical: Array.from({ length: 10 }, (_, i) => historyRow({ load_id: `h${i}` })), offers: Array.from({ length: 20 }, (_, i) => offerRow({ offer_id: `o${i}` })) } }
